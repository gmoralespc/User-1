
<ul class="nav nav-tabs" id="rightTab">
    <li class="active"><a href="#rt-help" data-toggle="tab">{{  Lang::get('appl.help') }}</a></li>
    <li><a href="#rt-settings" data-toggle="tab">{{  Lang::get('appl.settings') }}</a></li>
    <li><a href="#rt-request" data-toggle="tab">{{  Lang::get('appl.add') }}</a></li>
</ul>
<div class="tab-content">
    <div class="tab-pane active" id="rt-help">
        <fieldset>
            <legend>{{  Lang::get('appl.help') }}</legend>
        </fieldset>
    </div>
    <div class="tab-pane" id="rt-settings">
        <fieldset>
            <legend>{{  Lang::get('appl.settings') }}</legend>
        </fieldset>
    </div>
    <div class="tab-pane" id="rt-request">
        <fieldset>
            <legend>{{  Lang::get('appl.add') }}</legend>
        </fieldset>
    </div>
</div>
